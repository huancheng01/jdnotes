# Role
Act as a Senior Frontend Developer specializing in React and Rich Text Editors.

# Context
We have a Note-taking app with a 3-column layout. The visual shell is ready.
Now we need to implement the actual **Editor functionality** in the right column using **Tiptap**.

# Goal
Replace the static content in the Right Column with a fully functional Tiptap Editor component. It must look indistinguishable from a clean, static document but be editable.

# Tech Requirements
- **Library:** `@tiptap/react`, `@tiptap/starter-kit`, `@tiptap/extension-placeholder`
- **Styling:** Tailwind CSS + `@tailwindcss/typography` (Typography plugin).

# Specifications

## 1. The Editor Component (`Editor.tsx`)
- **Container:** Use a flexible container that takes up the remaining height (`flex-1 h-full overflow-y-auto`).
- **Styling:**
  - Use the `prose` class from Tailwind Typography (`prose prose-gray prose-lg max-w-3xl mx-auto`).
  - **Crucial:** Remove the default blue outline on focus (`focus:outline-none` inside `editorProps`).
  - Font: Set specific font families consistent with the design (Inter/Sans).
- **Placeholder:**
  - Title: "Untitled" (Rendered as an H1 input outside Tiptap or a specialized Tiptap extension).
  - Body: "Start writing..." (Use Tiptap Placeholder extension).

## 2. Visual Style (Linear/Notion Vibe)
- **Headings:** `text-gray-900 font-semibold tracking-tight`.
- **Paragraphs:** `text-gray-600 leading-relaxed`.
- **Lists:** Clean bullets, moderate padding.
- **Code Blocks:** Simple gray background (`bg-gray-100`), rounded corners, monospace font.

## 3. Data Integration (Mock)
- When clicking a note in the Middle List, update the editor content via `editor.commands.setContent()`.
- Implement a `useEffect` to handle content updates when the `activeNoteId` changes.

## 4. Interaction Details
- **Title Input:** Place a native `<input>` or `textarea` (auto-resizing) *above* the Tiptap editor for the Document Title. It should be big (`text-4xl`), bold (`font-bold`), and borderless.
- **Debounce:** (Optional hint) Mention that we will eventually need to debounce saves, but for now just handle state updates.

# Output
Provide the full code for:
1. `Editor.tsx` (The component).
2. Any necessary updates to the parent `App.tsx` to handle state passing (passing `content` prop to Editor).
3. The specific `npm install` command needed.